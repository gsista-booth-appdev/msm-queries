<h1> Actor # <%= @the_actor.id %> details </h1>
<a href="/actors" class="btn btn-block btn-outline-secondary">
  Go back
</a>

&nbsp;|&nbsp;

<a href="/delete_actor/<%= @the_actor.id %>" class="btn btn-block btn-outline-secondary">
  Delete actor
  <small>(Building this link is not required. It's an optional stretch goal; but, you have the tools if you want to give it a try.)</small>
</a>

<hr>

<dl>
  <dt>
    Name
  </dt>
  <dd>
    <%= @the_actor.name %>
  </dd>

  <dt>
    Dob
  </dt>
  <dd>
    <%= @the_actor.dob %>
  </dd>

  <dt>
    Bio
  </dt>
  <dd>
    <%= @the_actor.bio %>
  </dd>

  <dt>
    Image
  </dt>
  <dd>
    <img src="http://robohash.org/<%= @the_actor.id %>">
  </dd>

  <dt>
    Created at
  </dt>
  <dd>
    <%= time_ago_in_words(@the_actor.created_at) %> ago
  </dd>

  <dt>
    Updated at
  </dt>
  <dd>
    <%= time_ago_in_words(@the_actor.updated_at) %> ago
  </dd>
</dl>

<hr>

<h2> Filmography </h2>

<table border="1">
<% @character.each do |a_character| %>
  <tr>
    <td>
      <%= Movie.where({:id => a_character.movie_id }).first.title %>
    </td>
    <td>
      <%= Movie.where({:id => a_character.movie_id }).first.year %>
    </td>
    <td>
      <% d = Movie.where({ :id => a_character.movie_id }).first.director_id %>
      <%= Director.where({:id => d}).first.name %>
    </td>
    <td>
      <%= a_character.name %>
    </td>
    <td>
      <a href="/movies/<%= a_character.movie_id %>">
        Show details
      </a>
    </td>  
  </tr>
<% end %>
</table>
